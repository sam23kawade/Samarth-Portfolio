Samarth Portfolio — Vite + React + Tailwind (starter)

What's new in this version:
- Lottie animations integrated via lottie-player web component (CDN)
- Cursor spotlight effect (CSS + small JS) for premium feel
- Page mount/unmount transitions using Framer Motion
- Favicon / logo added at public/favicon.svg
- Axion project added to projects

How to run locally:
1. Replace `public/resume.pdf` with your actual resume PDF.
2. Install dependencies: `npm install`
3. Start dev server: `npm run dev`
4. Build: `npm run build`

Deploy: push to GitHub and connect to Cloudflare Pages (or any static host). Use `npm run build`.
